{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "eas-build-pre-install": "corepack enable && corepack prepare pnpm@9.5.0 --activate",
    "preinstall": "echo Using pnpm",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:android": "cd artifacts/mobile && npx eas-cli@latest build --platform android --profile preview",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^3.9.5",
    "typescript": "~5.9.3"
  },
  "packageManager": "pnpm@9.5.0"
}
